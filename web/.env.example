# Copy to .env.local and fill in to turn on real cross-device sync.
# Find these in your Supabase project → Settings → API.
# Leave blank and the app stores tables on-device (still fully usable).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
